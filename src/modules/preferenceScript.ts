export async function registerPrefsScripts(_window: Window) {
  // Preference binding is handled by zotero-plugin-scaffold
  // via the preference="" attributes in preferences.xhtml.
  // No custom script logic needed for automd preferences.
}
